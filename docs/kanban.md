# Kanban Board

## Спринт №1: Базовая инфраструктура и Создание упражнения (US-1)
**Цель спринта:** Настроить базовые слои приложения и полностью реализовать пользовательский сценарий создания кастомного упражнения (US-1).

### TO DO
- [ ] **Backend-1: Инициализация сервера, конфигурации и инфраструктуры БД**
  * **Файлы:** `main.go`, `internal/config/config.go`, `internal/database/db.go`
  * **Описание:**
    1. Создать минимальный каркас Go-приложения (`main.go`).
    2. Реализовать пакет `internal/config` для безопасного чтения строки подключения к БД (DSN) и порта сервера из переменных окружения (без хардкода).
    3. В пакете `internal/database` реализовать подключение к PostgreSQL через пул соединений `github.com/jackc/pgx/v5/pgxpool`.
    4. Написать функцию `RunMigrations()`, которая при старте проверяет наличие таблиц и выполняет DDL-скрипт (создание таблиц `exercises`, `challenges`, `workouts`, `user_achievements` и индексов из `architecture.md`).
  * **Ограничения:** Использовать чистый SQL. Сервер не должен падать, если база данных временно недоступна при старте (сделать 3 попытки переподключения).
- [ ] **Backend-2: API Handlers**
  * **Файл(ы):** `internal/handlers/exercise_handler.go`, `internal/handlers/router.go`
  * **Описание:**
    1. Реализовать `POST /api/exercises`.
       * Извлечь `X-User-Id` из заголовков (фоллбэк на `default_user_1`, если пусто).
       * Валидация: поле `name` не должно быть пустым. Если пусто — вернуть HTTP `400 Bad Request`.
       * Вызов `CreateExercise`. При успешном создании установить `is_custom = true` и вернуть HTTP `201 Created` с JSON созданного объекта. Если дубликат — вернуть `409 Conflict`.
    2. Реализовать `GET /api/exercises`.
       * Извлечь `X-User-Id`.
       * Вызов `GetExercises` и возврат массива JSON.
  * **Ограничения:** Писать подробные логи ошибок на сервере.
- [ ] **Frontend-1: API Client**
  * **Файл:** `js/api.js`
  * **Описание:**
    1. Добавить метод `getExercises()`, делающий `GET /api/exercises`.
    2. Добавить метод `createExercise(name)`, делающий `POST /api/exercises` с телом `{ "name": "..." }`.
  * **Ограничения:** Оба метода должны автоматически прикреплять заголовки `X-User-Id: default_user_1` и `Content-Type: application/json`.
- [ ] **Frontend-2: State Management (Store)**
  * **Файл:** `js/store.js`
  * **Описание:**
    1. Добавить в начальное состояние массив `exercises: []`.
    2. При инициализации приложения вызывать `api.getExercises()` и обновлять Store.
- [ ] **Frontend-3: UI Component (Challenge Form)**
  * **Файл:** `js/components/challenge/challenge-form.js`, `challenge-form.css`
  * **Описание:**
    1. Отрендерить выпадающий список (`<select>`) с упражнениями из Store.
    2. В конец списка добавить опцию `value="custom"` с текстом «Добавить свое...».
    3. Логика переключения: повесить слушатель `change` на селект. Если выбрано «Добавить свое», показывать скрытое ранее текстовое поле `<input type="text" id="custom-exercise-name">`. Иначе — скрывать.
    4. Логика сохранения (при сабмите формы создания челленджа):
       * Если селект в режиме "custom", сначала вызвать `api.createExercise(customName)`.
       * Обработать ответ: если API вернул ошибку 400 (пустое имя) или 409 (уже существует), показать пользователю уведомление (alert или текст под инпутом) и прервать отправку.
       * Если успешно: обновить Store новым списком упражнений, выбрать созданное упражнение в селекте и продолжить логику создания челленджа.
  * **Ограничения:**
    * Никаких фреймворков. Использовать Vanilla JS и `addEventListener`.
    * Стилизовать инпуты с использованием CSS Variables.
- [ ] **QA-1: API Тестирование (Postman / cURL)**
  * **Описание:**
    1. **TC-1.1 (Positive): Успешное создание**
       * *Steps:* `POST /api/exercises` с `{ "name": "Бег" }` и `X-User-Id: test_user`.
       * *Expected:* HTTP 201. В БД появилась запись с `is_custom = true` и `user_id = 'test_user'`.
    2. **TC-1.2 (Negative): Пустое имя (AC-3)**
       * *Steps:* `POST /api/exercises` с `{ "name": "" }` или `{"name": "   "}`.
       * *Expected:* HTTP 400 Bad Request. Запись не создана.
    3. **TC-1.3 (Negative): Дубликат имени**
       * *Steps:* Создать "Бег", затем повторно отправить `POST /api/exercises` с `{ "name": "Бег" }` и тем же `X-User-Id`.
       * *Expected:* HTTP 409 Conflict (или 400 с понятным текстом). Ошибка уровня БД перехвачена бэкендом.
- [ ] **QA-2: UI/UX Тестирование (Браузер)**
  * **Описание:**
    1. **TC-1.4 (Positive): Переключение UI (AC-1)**
       * *Steps:* Открыть форму создания челленджа. Выбрать в селекте «Добавить свое...».
       * *Expected:* Появляется текстовое поле для ввода названия. При выборе другого пункта из селекта текстовое поле снова скрывается.
    2. **TC-1.5 (Positive): Успешный флоу добавления (AC-2)**
       * *Steps:* Выбрать «Добавить свое», ввести «Планка», нажать Сохранить.
       * *Expected:* Отправляется запрос. Интерфейс обновляется: «Планка» появляется в селекте и автоматически выбирается.
    3. **TC-1.6 (Negative): Валидация пустого ввода (UI -> API)**
       * *Steps:* Выбрать «Добавить свое», оставить поле пустым, нажать Сохранить.
       * *Expected:* Отправляется запрос (или блокируется на фронте). Если запрос ушел, фронтенд корректно обрабатывает 400 ошибку от сети и показывает красное сообщение об ошибке, не крашась.

### IN PROGRESS

### QA / REVIEW

### DONE

